apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ostadchess-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: ostadchess

  source:
    repoURL: https://gitlab.com/kurusnsn-group/deterministic-ml-engine.git
    targetRevision: HEAD
    path: k8s/overlays/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: chess-staging

  # Automated sync for GitOps deployment
  syncPolicy:
    automated:
      prune: false  # Don't auto-delete resources not in git (safer)
      selfHeal: true  # Auto-fix drift from desired state
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground

  # Health check settings
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
